import React from 'react';
import { ScreenShell } from '../../components/ScreenShell';

// ⑥ カレンダー/一覧（screen.md 3.7）。実装フェーズで一覧・検索・週次インサイトを実装。
export function CalendarScreen() {
  return <ScreenShell title="過去の日記" subtitle="カレンダー / リスト（実装予定）" />;
}
