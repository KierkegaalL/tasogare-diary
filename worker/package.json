{
  "name": "tasogare-diary-worker",
  "description": "たそがれ日記 Claude 連携プロキシ（Cloudflare Workers / Spark プラン対応）",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.107.1"
  }
}
